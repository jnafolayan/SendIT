export const createSchema = {
  firstname: String,
  lastname: String,
  othernames: String,
  email: String,
  username: String,
  password: String,
};

export const loginSchema = {
  username: String,
  password: String,
};

export const fetchParcelsSchema = {
  userID: String,
};
